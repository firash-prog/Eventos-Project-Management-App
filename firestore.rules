rules_version = '2';

/**
 * EVENTOS V8.0 - ENTERPRISE FIRESTORE SECURITY RULES
 * Standard read/write rules for application collections and auth lookups.
 */
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

